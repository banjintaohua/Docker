version: "3.5"

services:
  php-fpm:
    build: ./build
    container_name: php-56
    restart: always
    ports:
      - 9002:9000
    volumes:
      - ../../Php/Task:/usr/local/www/data/taskv2:rw
      - ./logs/php-fpm:/var/log/php-fpm:rw
      - ./conf/php.ini:/usr/local/etc/php/php.ini:ro
      - ./conf/xdebug.ini:/usr/local/etc/php/conf.d/dexbug.ini:ro

networks:
  default:
    external:
      name: develop

