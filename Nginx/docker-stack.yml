version: "3.9"

services:
  acme:
    image: neilpang/acme.sh:3.0.2
    networks:
      - host
    volumes:
      - acme_data:/acme.sh:rw
      - ./conf/ssl/wildcard-certificate.crt:/etc/ssl/wildcard-certificate.crt:rw
      - ./conf/ssl/wildcard-certificate.key:/etc/ssl/wildcard-certificate.key:rw
    command: [ 'daemon' ]
    # command: source /run/secrets/acme_cloud_flare_env
    # command: acme.sh --register-account -m $CF_Email --issue -k ec-256 --dns dns_cf -d *.example.online
    # command: acme.sh --installcert --ecc -d *.example.online --fullchain-file /etc/ssl/wildcard-certificate.crt --key-file /etc/ssl/wildcard-certificate.key
    secrets:
      - acme_cloud_flare_env
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: any
        delay: 10s
        window: 60s
      placement:
        max_replicas_per_node: 1
        constraints:
          - node.hostname == ShangHai-CentOS
          - node.platform.os == linux

  nginx:
    image: nginx:latest
    ports:
      - target: 80
        published: 80
        mode: host
      - target: 443
        published: 443
        protocol: udp
        mode: host
    networks:
      - host
    deploy:
      mode: replicated
      replicas: 1
      restart_policy:
        condition: any
        delay: 10s
        window: 60s
      placement:
        max_replicas_per_node: 1
        constraints:
          - node.hostname == California-CentOS
          - node.platform.os == linux
    configs:
      - source: nginx_htpasswd
        target: /etc/nginx/.htpasswd
      - source: nginx_config
        target: /etc/nginx/nginx.conf
      - source: nginx_bark
        target: /etc/nginx/conf.d/bark.conf
      - source: nginx_blog
        target: /etc/nginx/conf.d/blog.conf
      - source: nginx_docker_registry
        target: /etc/nginx/conf.d/docker_registry.conf
      - source: nginx_elasticsearch
        target: /etc/nginx/conf.d/elasticsearch.conf
      - source: nginx_frps
        target: /etc/nginx/conf.d/frps.conf
      - source: nginx_gitlab
        target: /etc/nginx/conf.d/gitlab.conf
      - source: nginx_kibana
        target: /etc/nginx/conf.d/kibana.conf
      - source: nginx_minio
        target: /etc/nginx/conf.d/minio.conf
      - source: nginx_portainer
        target: /etc/nginx/conf.d/portainer.conf
      - source: nginx_qinglong
        target: /etc/nginx/conf.d/qinglong.conf
      - source: nginx_rabbitmq
        target: /etc/nginx/conf.d/rabbit.conf
      - source: nginx_shadowsocks
        target: /etc/nginx/conf.d/ss-websocket-http.conf
      - source: nginx_swagger
        target: /etc/nginx/conf.d/swagger.conf
      - source: nginx_vscode
        target: /etc/nginx/conf.d/vscode.conf
    secrets:
      - source: wildcard-certificate.crt
        target: /etc/ssl/wildcard-certificate.crt
      - source: wildcard-certificate.key
        target: /etc/ssl/wildcard-certificate.key

configs:
  nginx_htpasswd:
    name: nginx_htpasswd_v1
    file: ./conf/.htpasswd
  nginx_config:
    name: nginx_config_v1
    file: ./conf/nginx.conf
  nginx_bark:
    name: nginx_bark_v1
    file: ./conf/conf.d/bark.conf
  nginx_blog:
    name: nginx_blog_v1
    file: ./conf/conf.d/blog.conf
  nginx_docker_registry:
    name: nginx_docker_registry_v3
    file: ./conf/conf.d/docker_registry.conf
  nginx_elasticsearch:
    name: nginx_elasticsearch
    file: ./conf/conf.d/elasticsearch.conf
  nginx_frps:
    name: nginx_frps_v1
    file: ./conf/conf.d/frps.conf
  nginx_gitlab:
    name: nginx_gitlab_v3
    file: ./conf/conf.d/gitlab.conf
  nginx_kibana:
    name: nginx_kibana
    file: ./conf/conf.d/kibana.conf
  nginx_minio:
    name: nginx_minio
    file: ./conf/conf.d/minio.conf
  nginx_portainer:
    name: nginx_portainer
    file: ./conf/conf.d/portainer.conf
  nginx_qinglong:
    name: nginx_qinglong_v1
    file: ./conf/conf.d/qinglong.conf
  nginx_rabbitmq:
    name: nginx_rabbitmq_v1
    file: ./conf/conf.d/rabbit.conf
  nginx_shadowsocks:
    name: nginx_shadowsocks
    file: ./conf/conf.d/ss-websocket-http.conf
  nginx_swagger:
    name: nginx_swagger
    file: ./conf/conf.d/swagger.conf
  nginx_vscode:
    name: nginx_vscode
    file: ./conf/conf.d/vscode.conf

secrets:
  acme_cloud_flare_env:
    name: acme_cloud_flare_env
    file: ./conf/acme/acme_cloud_flare_env.sh
  wildcard-certificate.crt:
    name: wildcard-certificate.crt.v3
    file: ./conf/ssl/wildcard-certificate.crt
  wildcard-certificate.key:
    name: wildcard-certificate.key.v3
    file: ./conf/ssl/wildcard-certificate.key

volumes:
  acme_data:
    name: acme_data
    driver: local

networks:
  host:
    name: host
    external: true