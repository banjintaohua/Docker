version: "3.9"

services:
  node:
    image: node:14.18.2
    container_name: node
    restart: "no"
    working_dir: /var/www
    volumes:
      - ./data:/var/www:rw
    network_mode: host
    command: tail -f /dev/null