version: 0.1
log:
  fields:
    service: registry
storage:
  s3:
    accesskey: "minio"
    secretkey: "***REMOVED***"
    regionendpoint: "https://minio.***REMOVED***"
    bucket: docker
    secure: "false"
    region: ignored-for-minio
  delete:
    enabled: "true"
  redirect:
    disable: "false"
http:
  addr: 0.0.0.0:443
  tls:
    certificate: /certs/wildcard-certificate.crt
    key: /certs/wildcard-certificate.key
auth:
  htpasswd:
    path: /auth/.htpasswd
    realm: "Registry Realm"
health:
  storagedriver:
    enabled: true
    interval: 10s
    threshold: 3